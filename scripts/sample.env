# clone-events-sse helper configuration
# See docs/deployment.md for full documentation

# HTTP server listen address
LISTEN_ADDR=:4010

# Shared secret for webhook HMAC validation (must match bridge config)
WEBHOOK_SECRET=change-me-to-a-random-secret

# Number of recent events to buffer for new SSE subscribers
EVENT_BUFFER=200

# CORS whitelist: Comma-separated list of web origins allowed to connect to /events SSE endpoint
# This is NOT for Nostr relays - it's for browser-based SSE connections from the gittr UI
# Example: https://gittr.space,https://beta.gittr.space
ALLOW_ORIGINS=https://gittr.space

# Optional cache directory for blossom-fetch-helper (if used)
CACHE_DIR=/var/cache/gittr-blossom
